-- Used to remove all records with a score <= 5 from the table second_table in the specified database.
-- Remove all records with a score <= 5 from the table second_table.
DELETE FROM second_table WHERE score <= 5;
